# Core web stack
fastapi==0.110.0
uvicorn[standard]==0.29.0

# HTTP client for Google APIs, PSI, etc.
httpx==0.27.0

# Pydantic v2 (FastAPI depends on it)
pydantic==2.7.1
pydantic-core==2.18.2

# Optional: load local env vars if you need them
python-dotenv==1.0.1

# If you use Google Sheets/other extras later, re-add them here, pinned.
