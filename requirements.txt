# Core web stack
fastapi==0.110.0
uvicorn[standard]==0.29.0

# HTTP client for external APIs (PageSpeed, Google Places, etc.)
httpx==0.27.0

# Pydantic v2 (FastAPI 0.110 depends on it)
pydantic==2.7.1
pydantic-core==2.18.2

# Optional: load local env vars if you ever run locally with a .env file
python-dotenv==1.0.1
